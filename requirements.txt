# ── Core dashboard (Streamlit Cloud) ──────────────────────────────────────────
# All versions chosen to have pre-built wheels for Python 3.14

streamlit>=1.35.0
pandas>=2.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.2.0
python-dotenv>=1.0.0

# ── LLM / AI (pure Python, no native build required) ─────────────────────────
groq>=0.9.0
openai>=1.30.0
anthropic>=0.28.0
httpx>=0.27.0

# ── API server (optional, not used by Streamlit Cloud entrypoint) ─────────────
fastapi>=0.111.0
uvicorn>=0.30.0

# ── Testing (not installed on Streamlit Cloud, fine to keep) ─────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Browser automation (local only — not used by Streamlit Cloud entrypoint) ──
# playwright requires greenlet which has no Python 3.14 wheel yet.
# Keep commented out here; install locally with: pip install playwright
# playwright>=1.44.0
